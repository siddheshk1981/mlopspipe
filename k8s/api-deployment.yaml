apiVersion: apps/v1
kind: Deployment
metadata:
  name: loksai-api-deploy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api
  template:
    metadata:
      labels:
        app: api
    spec:
      containers:
      - name: api
        image: siddheshk1981/siddhesh-mlops830api:latest
        ports:
        - containerPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: loksai-api-svc
spec:
  selector:
    app: api
  ports:
  - port: 8000
    targetPort: 8000
